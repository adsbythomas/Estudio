---
import BaseLayout from '@layouts/BaseLayout.astro';
import Hero from '@components/Hero.astro';
import Counters from '@components/Counters.astro';
import Practice from '@components/Practice.astro';
import Manifiesto from '@components/Manifiesto.astro';
import PullQuote from '@components/PullQuote.astro';
import Team from '@components/Team.astro';
import RecentMatters from '@components/RecentMatters.astro';
import Timeline from '@components/Timeline.astro';
import News from '@components/News.astro';
import CrossLink from '@components/CrossLink.astro';
import Contact from '@components/Contact.astro';
import { legal } from '@lib/legal';

const base = import.meta.env.BASE_URL;
const withBase = (p: string) => (base.endsWith('/') ? base.slice(0, -1) : base) + p;
---
<BaseLayout
  practice="legal"
  title="PL Abogados — Derecho empresarial · Buenos Aires · 25 años"
  description="Estudio de abogados en Buenos Aires. Tributario, societario, laboral, aerocomercial, penal económico y ciudadanía. Trámites societarios y de ciudadanía online. Corresponsalías en distintos países.">

  <Hero
    practice="legal"
    titleLine1={legal.hero.titleLine1}
    titleLine2={legal.hero.titleLine2}
    lead={legal.hero.lead}
    altLabel="Ir al área contable"
    altHref={withBase('/contable')}
  />

  <Practice
    label={legal.practice.label}
    title={legal.practice.title}
    sub={legal.practice.sub}
    items={legal.practice.items}
    subpagePrefix={withBase('/areas')}
  />

  <Manifiesto />

  <CrossLink practice="legal" />

  <Team
    label={legal.team.label}
    title={legal.team.title}
    sub={legal.team.sub}
    members={legal.team.members}
    ctaText={legal.team.ctaText}
    subpagePrefix={withBase('/equipo')}
  />

  <PullQuote />

  <Timeline
    label={legal.timeline.label}
    title={legal.timeline.title}
    items={legal.timeline.items}
  />

  <RecentMatters />

  <News practice="legal" label="Novedades" title="Radar normativo" />

  <Counters
    label={legal.counters.label}
    title={legal.counters.title}
    sub={legal.counters.sub}
    items={legal.counters.items}
  />

  <Contact
    label={legal.contact.label}
    title={legal.contact.title}
    titleAccent={legal.contact.titleAccent}
    desc={legal.contact.desc}
    formAreas={legal.contact.formAreas}
    formAreaLabel="Área de consulta"
  />
</BaseLayout>
