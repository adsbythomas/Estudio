---
import BaseLayout from '@layouts/BaseLayout.astro';
import Hero from '@components/Hero.astro';
import Counters from '@components/Counters.astro';
import Practice from '@components/Practice.astro';
import Manifiesto from '@components/Manifiesto.astro';
import PullQuote from '@components/PullQuote.astro';
import Team from '@components/Team.astro';
import RecentMatters from '@components/RecentMatters.astro';
import Timeline from '@components/Timeline.astro';
import News from '@components/News.astro';
import CrossLink from '@components/CrossLink.astro';
import Contact from '@components/Contact.astro';
import VencimientosPreview from '@components/VencimientosPreview.astro';
import { accounting } from '@lib/accounting';

const base = import.meta.env.BASE_URL;
const withBase = (p: string) => (base.endsWith('/') ? base.slice(0, -1) : base) + p;
---
<BaseLayout
  practice="accounting"
  title="PL Contable — Contabilidad & Impuestos · Buenos Aires · 25 años"
  description="Área contable del estudio Pantarotto Lértora. Liquidación de impuestos, estados contables, auditoría, planificación fiscal, sueldos y contabilidad internacional.">

  <Hero
    practice="accounting"
    titleLine1={accounting.hero.titleLine1}
    titleLine2={accounting.hero.titleLine2}
    lead={accounting.hero.lead}
    altLabel="Ir al área legal"
    altHref={withBase('/')}
  />

  <Practice
    label={accounting.practice.label}
    title={accounting.practice.title}
    sub={accounting.practice.sub}
    items={accounting.practice.items}
    subpagePrefix={withBase('/servicios')}
  />

  <Manifiesto />

  <CrossLink practice="accounting" />

  <Team
    label={accounting.team.label}
    title={accounting.team.title}
    sub={accounting.team.sub}
    members={accounting.team.members}
    ctaText={accounting.team.ctaText}
    subpagePrefix={withBase('/equipo-contable')}
  />

  <PullQuote
    quote="Cuando el caso es complejo, la diferencia la hace el equipo: abogados y contadores leyendo el mismo expediente al mismo tiempo."
    attribution="Filosofía del estudio"
  />

  <Timeline
    label={accounting.timeline.label}
    title={accounting.timeline.title}
    items={accounting.timeline.items}
  />

  <RecentMatters />

  <News practice="accounting" label="Novedades" title="Radar impositivo" />

  <VencimientosPreview />

  <Counters
    label={accounting.counters.label}
    title={accounting.counters.title}
    sub={accounting.counters.sub}
    items={accounting.counters.items}
  />

  <Contact
    label={accounting.contact.label}
    title={accounting.contact.title}
    titleAccent={accounting.contact.titleAccent}
    desc={accounting.contact.desc}
    formAreas={accounting.contact.formAreas}
    formAreaLabel="Servicio requerido"
  />
</BaseLayout>
