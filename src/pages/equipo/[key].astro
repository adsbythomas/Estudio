---
import BaseLayout from '@layouts/BaseLayout.astro';
import { legal } from '@lib/legal';

export function getStaticPaths() {
  return legal.team.members
    .filter((m) => !!m.key)
    .map((member) => ({
      params: { key: member.key as string },
      props: { member },
    }));
}

const { member } = Astro.props;

const base = import.meta.env.BASE_URL;
const withBase = (p: string) => (base.endsWith('/') ? base.slice(0, -1) : base) + p;
---
<BaseLayout
  practice="legal"
  title={`${member.name} — PL Abogados`}
  description={`Perfil profesional de ${member.name}, ${member.role} del Estudio Pantarotto Lértora & Asocs.`}
  landing={false}
>
  <section class="member-hero">
    <div class="shell">
      <a href={withBase('/#equipo')} class="member-back">
        <span aria-hidden="true">←</span> Volver al equipo
      </a>

      <div class="member-grid">
        <div class="member-photo">
          <span class="member-photo-fallback" aria-hidden="true">{member.initials}</span>
          {member.photo && (
            <img
              src={withBase(member.photo)}
              alt={`Foto de ${member.name}`}
              style={`object-position: ${member.photoFocal ?? 'center top'};`}
              onerror="this.style.display='none'"
            />
          )}
        </div>

        <div class="member-info">
          <div class="member-role">{member.role}</div>
          <h1 class="member-name">{member.name}</h1>
          {member.spec && <div class="member-spec">{member.spec}</div>}

          {member.email && (
            <div class="member-contact">
              <span class="member-contact-label">Info</span>
              <a href={`mailto:${member.email}`} class="member-contact-link">{member.email}</a>
            </div>
          )}

          <div class="member-bio">
            <p>Aca va la informacion de cada uno.</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .member-hero {
    padding: 8rem 0 6rem;
  }
  .member-back {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: ui-monospace, 'SF Mono', Menlo, monospace;
    font-size: 0.7rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--fg-muted);
    margin-bottom: 3rem;
    transition: color .25s ease;
  }
  .member-back:hover { color: var(--brand-500); }

  .member-grid {
    display: grid;
    gap: 2.5rem;
    grid-template-columns: 1fr;
    align-items: start;
  }
  @media (min-width: 768px) {
    .member-grid {
      grid-template-columns: minmax(260px, 360px) 1fr;
      gap: 4rem;
    }
  }

  .member-photo {
    position: relative;
    aspect-ratio: 1 / 1;
    border-radius: 12px;
    overflow: hidden;
    background: linear-gradient(135deg,
      color-mix(in oklab, var(--brand-500) 22%, var(--surface-2)),
      var(--surface-2));
    border: 1px solid var(--border);
  }
  .member-photo-fallback {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    font-family: 'Montserrat', Arial, Helvetica, sans-serif;
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 700;
    color: var(--brand-500);
    letter-spacing: 0.04em;
    opacity: 0.55;
  }
  .member-photo img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    /* Anclamos al top para no recortar la cabeza en retratos. */
    object-position: center top;
    z-index: 1;
  }

  .member-role {
    font-family: ui-monospace, 'SF Mono', Menlo, monospace;
    font-size: 0.72rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--brand-500);
    margin-bottom: 1rem;
  }
  .member-name {
    font-family: 'Montserrat', Arial, Helvetica, sans-serif;
    font-size: clamp(2rem, 5vw, 3.4rem);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--fg);
    margin: 0 0 1rem;
  }
  .member-spec {
    font-size: 1rem;
    color: var(--fg-muted);
    margin-bottom: 1.25rem;
    line-height: 1.5;
  }
  .member-contact {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
    padding: 0.7rem 0.9rem;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--surface);
    width: fit-content;
  }
  .member-contact-label {
    font-family: ui-monospace, 'SF Mono', Menlo, monospace;
    font-size: 0.62rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--fg-subtle);
    font-weight: 700;
  }
  .member-contact-link {
    font-family: 'Montserrat', system-ui, sans-serif;
    font-size: 0.95rem;
    color: var(--brand-500);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color .25s ease;
  }
  .member-contact-link:hover {
    border-bottom-color: var(--brand-500);
  }
  .member-bio {
    font-size: 1.02rem;
    line-height: 1.7;
    color: var(--fg-muted);
    border-top: 1px solid var(--border);
    padding-top: 1.5rem;
    margin-top: 0.5rem;
  }
  .member-bio p { margin: 0 0 1rem; }
</style>
